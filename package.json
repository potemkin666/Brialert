{
  "name": "brialert-pages",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.18.1"
  },
  "scripts": {
    "validate:feed-data": "node ./scripts/validate-feed-data.mjs",
    "build:feeds": "node ./scripts/build-live-feed.mjs",
    "test": "node --test ./tests/*.test.mjs"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "fast-xml-parser": "^4.5.5"
  }
}
